<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".SmartisanWeatherApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/weather_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/weather_icon"
        android:supportsRtl="false"
        android:theme="@style/MainActivityTheme"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:theme="@style/MainActivityTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.search.SearchCityActivity"
            android:exported="false"
            android:label="@string/add_city"
            android:screenOrientation="portrait"
            android:theme="@style/weather_theme"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ui.citylist.CityListActivity"
            android:exported="false"
            android:label="@string/city_list"
            android:screenOrientation="portrait"
            android:theme="@style/weather_theme" />

        <activity
            android:name=".ui.alert.WeatherAlertActivity"
            android:exported="false"
            android:label="@string/weather_alert_title"
            android:screenOrientation="portrait"
            android:theme="@style/weather_theme" />

        <activity
            android:name=".appwidget.WeatherWidgetConfigureActivity"
            android:excludeFromRecents="true"
            android:exported="true"
            android:label="@string/weather_widget_configure_title"
            android:theme="@style/WeatherWidgetConfigureTheme">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".appwidget.WeatherWidgetProvider"
            android:exported="false"
            android:label="@string/weather_widget_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/weather_widget_info" />
        </receiver>

        <!-- The app owns one in-process Room singleton and never enables
             multi-instance invalidation, so its cross-process service is unreachable. -->
        <service
            android:name="androidx.room3.MultiInstanceInvalidationService"
            tools:node="remove" />
    </application>

</manifest>
